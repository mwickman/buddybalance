<!DOCTYPE html>
<html>
<head>
  <title>Buddy Balance | <%= yield(:title) %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<%= render 'layouts/header' %>
<div class="container">
  <% flash.each do |k, v| %>
    <div class="alert alert-<%= k %>"><%= v %></div>
  <% end %>
  <%= yield %>
</div>
<%= debug(params) if Rails.env.development? %>
</body>
</html>
