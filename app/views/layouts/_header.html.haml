%header.navbar.navbar-fixed-top
  .navbar-inner
    .container
      = link_to "Buddy Balance", root_path, class: "brand"
      = link_to "About", about_path
      - if signed_in?
        = link_to "Sign out", signout_path, method: "delete" 
      - else
        = link_to "Sign in", signin_path
